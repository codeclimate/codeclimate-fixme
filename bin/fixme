#!/usr/bin/env node

var fs = require('fs');
var FixMe = require('../lib/fixme');
var config;

fs.readFile('/config.json', function(err, data) {
  if (!err) {
    config = JSON.parse(data);
  }

  var fixer = new FixMe();
  fixer.run(config);
});
